@model TradeSpace.Models.Cart

@{
    ViewData["Title"] = "Мій кошик";
}

<div class="container mt-4">
    <h2>Кошик</h2>

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">
            @TempData["ErrorMessage"]
        </div>
    }

    @if (Model == null || !Model.Items.Any())
    {
        <div class="alert alert-info">
            Ваш кошик порожній. <a asp-controller="Products" asp-action="Index">Перейти до каталогу</a>
        </div>
    }
    else
    {
        <table class="table align-middle">
            <thead>
                <tr>
                    <th>Товар</th>
                    <th>Ціна</th>
                    <th>Кількість</th>
                    <th>Сума</th>
                    <th>Дії</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>@(item.Product?.Title ?? "Невідомий товар")</td>
                        <td>@(item.Product?.Price.ToString("C") ?? "0")</td>
                        <td>@item.Quantity</td>
                        <td>@((item.Quantity * (item.Product?.Price ?? 0)).ToString("C"))</td>
                        <td>
                            <!-- Кнопка видалення конкретного товару -->
                            <form asp-controller="Cart" asp-action="RemoveItem" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="cartItemId" value="@item.Id" />
                                <button type="submit" class="btn btn-sm btn-danger">Видалити</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="d-flex justify-content-between align-items-center mt-3">
            <!-- Очищення всього кошика -->
            <form asp-controller="Cart" asp-action="Clear" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" name="cartId" value="@Model.Id" />
                <button type="submit" class="btn btn-outline-danger">Очистити весь кошик</button>
            </form>

            <!-- Кнопка оформлення замовлення -->
            <a asp-controller="Orders" asp-action="Checkout" class="btn btn-success">
                Оформити замовлення
            </a>
        </div>
    }
</div>