@model TradeSpace.Models.User
@using TradeSpace.Models

@{
    ViewData["Title"] = "Личный кабинет";
}

<div class="container mt-4">
    <h2>Личный кабинет</h2>
    <hr />

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success">
            @TempData["SuccessMessage"]
        </div>
    }

    <div class="mb-4">
        <h4><strong>@Model.FirstName @Model.LastName</strong></h4>
        <p><strong>Email:</strong> @Model.Email</p>
        <p><strong>Номер телефону:</strong> @(string.IsNullOrEmpty(Model.PhoneNumber) ? "Не вказано" : Model.PhoneNumber)</p>
        <p><strong>Роль:</strong> @Model.Role</p>
        <p><strong>Зарегистрирован:</strong> @Model.CreatedAt.ToString("dd.MM.yyyy")</p>

        <div class="mt-3">
            <a asp-action="EditProfile" class="btn btn-outline-primary btn-sm">Изменить имя</a>
            <a asp-action="ChangePassword" class="btn btn-outline-secondary btn-sm">Изменить пароль</a>

            @if (Model.Role == UserRole.Admin)
            {
                <a asp-controller="Admin" asp-action="Users" class="btn btn-danger btn-sm ms-2">Управление пользователями</a>
            }
            else
            {
                <a asp-controller="Support" asp-action="Index" class="btn btn-outline-info btn-sm ms-2">Поддержка (Чат с админом)</a>
            }
            @if (Model.Role == UserRole.Admin)
            {
                <a asp-controller="Admin" asp-action="Users" class="btn btn-danger btn-sm ms-2">Управління користувачами</a>
                <a asp-controller="Admin" asp-action="Tickets" class="btn btn-warning btn-sm ms-2">Всі тикети</a>
            }
        </div>
    </div>

    <hr />

    <div class="mb-4">
        <h4>Мои магазины</h4>
        <p class="text-muted">У вас пока нет созданных магазинов.</p>
    </div>

    <div class="mb-4">
        <h4>Мои заказы</h4>
        <p class="text-muted">История заказов пуста.</p>
    </div>
</div>