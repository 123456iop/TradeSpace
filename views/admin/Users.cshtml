@using TradeSpace.Models
@model IEnumerable<TradeSpace.Models.User>

@{
    ViewData["Title"] = "Управління користувачами";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Управління користувачами</h2>
        <!-- Кнопка тепер веде назад в кабінет -->
        <a asp-controller="Account" asp-action="Index" class="btn btn-outline-secondary">Назад до кабінету</a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>Користувач</th>
                            <th>Email</th>
                            <th>Дата реєстрації</th>
                            <th>Поточна роль</th>
                            <th>Змінити роль</th>
                            <th>Дії</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td>
                                    <strong>@user.FirstName @user.LastName</strong>
                                </td>
                                <td>@user.Email</td>
                                <td>@user.CreatedAt.ToString("dd.MM.yyyy HH:mm")</td>
                                <td>
                                    @if (user.Role.ToString() == "Admin")
                                    {
                                        <span class="badge bg-danger">Admin</span>
                                    }
                                    else if (user.Role.ToString() == "Seller")
                                    {
                                        <span class="badge bg-success">Seller</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">Customer</span>
                                    }
                                </td>
                                <td>
                                    <form asp-action="ChangeRole" method="post" class="d-flex gap-2" style="max-width: 260px;">
                                        <input type="hidden" name="userId" value="@user.Id" />
                                        <select name="newRole" class="form-select form-select-sm">
                                            @foreach (UserRole role in Enum.GetValues(typeof(UserRole)))
                                            {
                                                if (role.ToString() == user.Role.ToString())
                                                {
                                                    <option value="@role" selected="selected">@role</option>
                                                }
                                                else
                                                {
                                                    <option value="@role">@role</option>
                                                }
                                            }
                                        </select>
                                        <button type="submit" class="btn btn-primary btn-sm">Зберегти</button>
                                    </form>
                                </td>
                                <td>
                                    <form asp-action="DeleteUser" method="post" onsubmit="return confirm('Ви впевнені, що хочете видалити цього користувача?');">
                                        <input type="hidden" name="userId" value="@user.Id" />
                                        <button type="submit" class="btn btn-outline-danger btn-sm">Видалити</button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>